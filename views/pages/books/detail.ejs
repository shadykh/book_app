<h1>Shows2</h1>
<% taskResults.forEach((book, idx)=>{ %> 
    <div>
        <img src="<%= book.imageLinks %>" alt="">
        <h3><%= book.title %></h3>
        <h4><%= book.authors %></h4>
        <p><%= book.description %></p>
    </div>
<% }) %> 